# shared

Utilities shared across tools: config loading, logging, HTTP clients,
rate limiting, caching, etc.

Keep modules here small and focused. If something is only used by one tool,
it belongs in that tool's directory instead.
