# apis

Helpers and lightweight clients for interacting with external APIs.

Use this directory for one-off API utilities that don't need to be exposed
as an MCP server. If a utility grows into something reusable by AI agents,
consider promoting it to `mcp-servers/`.
