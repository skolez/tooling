# crawlers

Web crawling and scraping tools.

Each crawler lives in its own subdirectory with its own dependencies and a
README describing: inputs, outputs, rate-limit/robots handling, and how an
AI agent should invoke it.
